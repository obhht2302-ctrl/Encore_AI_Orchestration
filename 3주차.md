# 3주차

## 3주차에 배운 것

### 1. 스마트앱 컨트롤 해제
- 마이크로소프트가 보안을 강화하여서 개발에 매번 경고문을 날려오는 것을 해제하는 법. personal folder에 이미지 파일 있다.

### 2. Add as Python Project
- 프로젝트 폴더를 오른쪽 클릭한 뒤 add as python project 클릭하면 프로젝트 폴더 위로 .vscode가 뜨고 해당 프로젝트 터미널 열때 자동으로 가상환경으로 연계된다.

### 3. 다양한 프론트엔드 화면
- 선 그래프 그려보기
- 막대 그려보기
- 대시보드로 학생 점수 필터하기
- 차트로 도표 보여주기
- 사이드바 추가하기
- session state의 정의
- streamlit 패키지

### 4. Streamlit.io
- https://streamlit.io/cloud
- 프론트엔드 배포 사이트. Render는 백엔드 서버 배포 사이트.

### 5. 데이터 모아둔 사이트
- https://www.data.go.kr
- 각종 데이터를 모아둔 사이트. 데이터를 찾을 때 좋다.

### 6. 내 아이피 주소로 백엔드 서버 띄우기
- uvicorn main:app --reload. 이건 내 컴퓨터 안에서만 백엔드 서버를 띄우는 명령어.
- uvicorn main:app --host(내 아이피 주소) --port 8000. 이건 내 컴퓨터의 IP 주소로 백엔드 서버를 띄우는 명령어. 이렇게 띄워두면 다른 컴퓨터에서도 내 컴퓨터로 띄워둔 백엔드 서버를 사용할 수 있다.
- 윈도우 powershell에서 IPconfig를 입력하면 IPv4 주소가 내 컴퓨터의 IP 주소이다.

### 7. 프론트엔드와 백엔드 연동하기
- 프론트엔드를 만들어둔 곳에서 httpx 패키지를 이용하여 백엔드 서버의 주소를 변수로 만들어두면 백엔드 서버와 프론트엔드가 연동이 된다. 프론트엔드는 화면을 띄우고 백엔드 서버는 DB를 연결한다.
- 내가 만든 백엔드 서버말고 다른 사이트의 백엔드 서버와도 연결해 보기. 날씨 웹사이트를 백엔드 사이트로 설정해서 정보를 프론트엔드로 출력하기
- 백엔드의 LLM을 프론트엔드와 연동하여 프론트엔드 화면에 챗 응답 띄워보기.

### 8. 단어 하나하나를 따지면서 개발하기
- 이젠 디테일을 신경써야 할 때다. 문맥과 흐름을 파악하고 단어 하나하나를 따지기.
- 이젠 각 부분들에 대해서 디테일하고 면밀하게 하나씩 익혀야 할 때이다. 특히 프론트엔드에서는 문법을 제대로 익혀서 정보를 나타낼 줄 알아야 한다.

### 9. 디렉토리가 바뀌면 해야 하는 일
- 프로젝트 폴더의 디렉토리가 바뀌면 반드시 .venv와 .git을 없애고 다시 설치해야 한다. 왜냐하면 설치할 때 디렉토리가 저장되어 있기 때문에 새로 지정해야만 한다.

### 10. 프로젝트 폴더 구조
- 프로젝트 폴더를 만들 때 꼭 넣어야 하는 파일들은 plan.md(건축 설계도), setup.md(건물을 짓기 전에 작업자가 준비하는 메뉴얼), Readme.md(해당 프로젝트에 대한 안내서)

### 11. 프론트 엔드, 백엔드, DB 연동하기
- 프론트 엔드에서 정보를 입력받고 백엔드로 전송 후 DB에 해당 정보를 저장해두고 꺼내쓰기.
- 흐름 이해하기.
- 폴더 구조 익히기.
- 각 파일별 문법들.
- 예외 처리 작성하기.
- 프론트엔드에서 회원가입을 하면 DB에 저장되고 로그인 후 상품 등록을 하면 DB에 저장된다.