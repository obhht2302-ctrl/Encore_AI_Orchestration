# 4주차

## 4주차에 배운 것

### 1. DB에 데이터 저장 시 데이터 암호화하기
- password.py를 따로 만들어서 회원가입했을 때 비번이 저장된 DB에서 뜨지 않도록 암호화하기

### 2. 아이디어를 내서 백엔드와 프론트엔드에 기능 추가하기
- CRUD뿐만 아니라 검색 기능이라던지 비번 암호화라던지 그런 기능들을 아이디어를 스스로 내서 구현할 줄 알아야 한다.

### 3. Auth 기능 추가하기(회원 정보 수정)
- 회원가입 후에 로그인해서 회원 정보를 수정하는 기능 추가하기. 아이디는 그대로 두고 비번과 이름만 수정해서 DB에 업데이트하기.

### 4. 프론트엔드, 백엔드로 상품 관리 CRUD 구현하기
- 프론트엔드, 백엔드, DB를 연동해서 화면 띄우고 정보를 처리해서 DB까지 연계되는 CRUD를 구현하기.

### 5. 관리자 프론트엔드, 사용자 프론트엔드 구현 및 동일한 백엔드 사용하기
- 관리자 프론트엔드와 사용자 프론트엔드를 구현하고 두가지의 화면을 동일한 백엔드로 사용하기.

### 6. 상품 등록 시 이미지 파일 넣어보기
- 상품 등록할 때 이미지 파일도 같이 넣어서 프론트엔드에 띄워보기.

### 7. 상품 CRUD 외에 상품 검색 기능 추가하기
- 이미 DB에 들어있는 상품을 검색해보는 기능 프론트엔드로 구현하기. query parameter의 기능을 통해서 구현해야 한다.

### 8. 백엔드와 연동된 LLM과의 대화를 위한 프론트엔드 구현하기
- 백엔드와 연동된 LLM을 프론트엔드로 구현하기. LLM과의 대화하는 건 이미 백엔드에서 기능이 구현되었고 이제 사용자 프론트엔드에서도 로그인한 후에 LLM과 채팅할 수 있도록 구현하기.

### 9. LLM 프론트엔드 기능 추가하기(멀티턴, 대화 누적, 메시지 수, 대화 초기화, 대화 내용 DB에 담기)
- LLM 프론트엔드에서 기능 추가하기. 멀티턴, 그동안 했던 대화 화면으로 누적, 했던 대화 내용들 DB에 아이디별로 담기, 지금까지 대화했던 메시지 수 띄우기, 대화 초기화하기, 저장된 대화 삭제하기.